#include "decompose.h"

#include "common.h"

WaveDecomposition decompose(const std::vector<double>& signal,
                            const std::vector<double>& frequencies)
{
    unused(signal);
    unused(frequencies);

    // TODO: impl

    return WaveDecomposition();
}
